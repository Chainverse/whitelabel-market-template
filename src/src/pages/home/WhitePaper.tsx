import React from 'react';
import { Col, Row } from 'react-bootstrap';
import { NavLink } from 'react-router-dom';

const Section: React.FC = () => {
	return (
		<React.Fragment>
			<div className="bx-section" id="bx-white-paper" >
					
			</div>
		</React.Fragment>
	);
}

export default Section;
